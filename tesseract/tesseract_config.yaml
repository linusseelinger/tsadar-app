# Tesseract configuration file
# Generated by tesseract 0.5.0+4.gaf08a93 on 2025-01-06T12:05:46.850817

name: "tsadar-tesseract"
version: "v0.1.2"
description: ""

build_config:

  # Platform to build the container for. In general, images can only be executed
  # on the platform they were built for.
  target_platform: "linux/amd64"

  # Data to copy into the container, relative to the project root
  package_data:
   - [1d-defaults.yaml, 1d-defaults.yaml]
   - [1d-inputs.yaml, 1d-inputs.yaml]
